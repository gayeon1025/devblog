# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Deployment

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

permissions:
  contents: read

jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v3
#     - name: Set up JDK 17
#       uses: actions/setup-java@v3
#       with:
#         java-version: '17'
#         distribution: 'temurin'
#     - name: Build with Gradle
#       uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
#       with:
#         arguments: build
#     - uses: actions/upload-artifact@v3
#       with:
#         name: jar
#         path: build/libs
        
#   send-jar:
#     needs: build
#     runs-on: ubuntu-latest
#     steps: 
#       - name: Download jar
#         uses: actions/download-artifact@v3
#         with:
#           name: jar
#       - name: Send jar to remote server
#         uses: appleboy/scp-action@master
#         with:
#           host: 144.24.77.45
#           username: opc
#           source: "devblog-0.0.1-SNAPSHOT.jar"
#           target: "/home/opc"
#           key: ${{ secrets.PRIVATE_KEY }}
  
  run-app:
#     needs: send-jar
    runs-on: ubuntu-latest
    steps:
      - name: temp
        shell: bash
        run: ls
      - name: move deploy.sh
        uses: appleboy/scp-action@master
        with:
          host: 144.24.77.45
          username: opc
          source: "deploy.sh"
          target: "/home/opc"
          key: ${{ secrets.PRIVATE_KEY }}
#       - name: move deploy.sh
#         uses: appleboy/scp-action@master
#         with:
#           username: opc
#           host: 144.24.77.45
# #           host: ${{ secrets.HOST }}
#           key: ${{ secrets.PRIVATE_KEY }}
#           source: "deploy.sh"
#           target: "/home/opc"
#           #strip_components: 2
      - name: execute script
        uses: appleboy/ssh-action@master
        with:
          username: opc
          host: 144.24.77.45
          key: ${{ secrets.PRIVATE_KEY }}
          script_stop: true
          script: cd /home/opc && chmod +x deploy.sh && ./deploy.sh
#         run: scp ~/devblog/build/libs/devblog-0.0.1-SNAPSHOT.jar opc@144.24.77.45:/home/opc

#         run: | 
#           scp ~/devblog/build/libs/devblog-0.0.1-SNAPSHOT.jar opc@144.24.77.45:/home/opc
#       - name: Send jar to remote server
      
    
    
